services:
  partner2:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    container_name: partner2-dashboard
    ports:
      - "3112:3112"
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      PORT: 3112
      HOSTNAME: 0.0.0.0
      AUTH_REQUIRE_REDIS: "false"
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://127.0.0.1:3112/partner2 || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
